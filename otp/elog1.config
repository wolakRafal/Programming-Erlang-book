%% no tty
[{sasl, [
  {sasl_error_logger, false}
]}].
%% If we start the system with this configuration file,
%% we’ll get only error reports and not progress reports and so on.
%% All these error reports are only in the shell.